# Node.js Express Practice
> Create "db-config.json" under /settings folder.
>> "db-config.json" FORMAT

    {
        "host": "hostname",
        "port": port,
        "user": "username",
        "password": "password",
        "database": "database"
    }

## Script
> npm start